<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard - Mental Health App</title>
  <link 
    rel="stylesheet" 
    href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
  >
  <style>
    body {
      background-color: transparent;
      background-image: url('/images/coklat2.png');
      background-size: cover;
      background-position: center;
      color: #fff;
    }

    .jumbotron {
      background: rgba(255, 255, 255, 0.15);
      border-radius: 20px;
      padding: 2rem;
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      color: #fff;
      border: 1px solid rgba(255, 255, 255, 0.18);
    }

    /* Menghilangkan style background default card agar tidak bentrok */
    .card {
      border: none;
      transition: 0.3s;
    }

    /* Style khusus untuk kotak fitur (Mood Tracker, 1-on-1 Sessions, Journaling) */
    .feature-card {
      background-color: #D2B48C; /* Pastel coklat */
      color: #4E342E;           /* Warna teks dark brown */
    }

    .feature-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 0 10px rgba(138, 43, 226, 0.4);
    }

    .feature-card .card-title,
    .feature-card .card-text {
      color: #4E342E;
    }

    .btn-outline-light {
      border-color: #fff;
    }

    .btn-outline-light:hover {
      background-color: #fff;
      color: #333;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="jumbotron text-center">
      <h1 class="display-4">How's your mood today <%= user.username %> 👋</h1>
      <p class="lead text-light">Welcome.</p>
      <hr class="my-4 bg-light">
      <p class="text-light"></p>

      <div class="row mt-4">
        <!-- Mood Tracker -->
        <div class="col-md-4 mb-3">
          <div class="card feature-card h-100">
            <div class="card-body text-center">
              <h5 class="card-title">Mood Tracker</h5>
              <p class="card-text">Track how you feel daily.</p>
              <a href="/mood" class="btn btn-outline-light mt-2">Mulai</a>
            </div>
          </div>
        </div>

        <!-- List to Do -->
        <div class="col-md-4 mb-3">
          <div class="card feature-card h-100">
            <div class="card-body text-center">
              <h5 class="card-title">List to do</h5>
              <p class="card-text">Start your day with Productivity.</p>
              <a href="/todo" class="btn btn-outline-light mt-2">Mulai List to do</a>
            </div>
          </div>
        </div>

        <!-- Journaling -->
       <!-- Journaling -->
<div class="col-md-4 mb-3">
  <div class="card feature-card h-100">
    <div class="card-body text-center">
      <h5 class="card-title">Journaling</h5>
      <p class="card-text">Reflect on your day and discover new insights.</p>
      <a href="/journal/new" class="btn btn-outline-light mt-2">Mulai Journaling</a>
    </div>
  </div>
</div>


        <a href="/auth/logout" class="btn btn-outline-light mt-3">Logout</a>

    </div>
  </div>
</body>
</html>
